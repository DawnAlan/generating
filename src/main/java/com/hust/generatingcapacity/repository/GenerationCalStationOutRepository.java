package com.hust.generatingcapacity.repository;

import com.hust.generatingcapacity.entity.GenerationCalStationOut;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GenerationCalStationOutRepository extends JpaRepository<GenerationCalStationOut, Integer> {
}
